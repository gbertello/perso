version: '3.5'
services:
  node_prod:
    container_name: perso_node_prod
    build: node/
    restart: always
    environment:
      - ENV=prod
      - USERNAME=${USER}
      - PASSWORD=${PASSWORD}
      - MONGO_URI=mongodb+srv://${USER}:${PASSWORD}@${MONGO_URI}
    networks:
      - prod
networks:
  prod:
    external:
      name: prod
